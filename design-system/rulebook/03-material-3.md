# 03 · Material 3 (what we borrow)
Tonal elevation (surface tint by elevation) for dark mode instead of hard shadows. State layers (hover 8% / pressed 12% overlays). Motion: emphasized easing, container transform for card→detail. Dynamic type scale roles (Display/Headline/Title/Body/Label). Components as tokens. (We keep our own metal/glass aesthetic, not stock M3 look.)
