---
description: Taste & craft pass on a built screen — applies Ashish's design skills to lift a checklist-compliant screen to ground-truth material quality. Runs after figma-master build, before design-critique.
argument-hint: [screen name]
---
Run a taste/craft pass on "$ARGUMENTS". Goal: close the gap between "DNA-checklist compliant" and "looks like it belongs beside ✦ 7 · Design." Use Ashish's skills — `ui-ux-pro-max`, `emil-design-eng`, `impeccable`, `taste`, `trends` — and reference/ART_DIRECTION.md (v2 material system).

Do, in order:
1. **Benchmark diff:** open `✦ 7 · Design` (Home + Onboarding) in the working file. List concretely where "$ARGUMENTS" falls short of that page's craft — surface depth, grain, glass, copper-metal treatment, rim-glow, spacing rhythm, numeral treatment. Be specific per element.
2. **Material pass (figma-master executes the edits):** apply the ART_DIRECTION material system —
   - flat fills → layered obsidian depth + ~3–5% grain on elevated surfaces
   - toggles/chips/tooltips → frosted glass recipe (blur + hairline highlight + copperShimmer stroke)
   - primary CTA → brushed-copper metal (r8, sheen, inner highlight, ink text)
   - add ONE warm copper rim-glow to the hero element only
   - hero numeral → optional subtle metallic, cream Tabular, Indian grouping
   - any imagery → copper-sepia desaturation
3. **Restraint pass:** cut density. One focal point. If any element isn't earning its place (extra tile, redundant toggle, unrequested table), remove or defer it and say why.
4. **Anti-drift check:** confirm ZERO purple/blue/teal/lime accents crept in from the moodboard. Copper is the only accent. Every colour bound to a token.
5. **The brave move:** name and apply one distinctive, defensible choice for this screen (ART_DIRECTION "fill per area").
6. Re-screenshot. Show before/after framing. Verify all new surfaces use tokens/variables (no hardcoded values), and log any new components to design-system/components-added.md.

Then hand to design-critique. Report in Hinglish with the before/after screenshots.
