---
description: Run APCA contrast audit and report
argument-hint: [target: "text roles" | screen name]
---
Run `npm run apca` for "$ARGUMENTS". Paste the raw script output, then summarize: which role pairs pass body threshold (|Lc| ≥ 75), which are large/icon-only (≥60/≥45), any real fails with the specific token pair and the fix (tint %, role swap, or size bump). Never summarize without the raw output present.
