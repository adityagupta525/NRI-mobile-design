---
description: Build design tokens and verify the sanity gate.
---
Run `npm run tokens:build` then `npm run lint:design`. Paste raw output.
The build MUST print: `sanity: space-4=16px, touch-min=44px`.
