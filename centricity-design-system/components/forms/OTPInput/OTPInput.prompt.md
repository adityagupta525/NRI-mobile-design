OTPInput is the 6-digit segmented code entry for mobile/email OTP during KYC and login — auto-advances focus, backspace steps back.

```jsx
<OTPInput length={6} onComplete={(code) => verify(code)} />
```
