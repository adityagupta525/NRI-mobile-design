TextInput is the base single-line field (PAN, name, email, address) — raised surface, copper focus ring, error state.

```jsx
<TextInput label="PAN Number" placeholder="ABCDE1234F" necessity="required" />
<TextInput label="Email" errorText="Enter a valid email address" />
```
