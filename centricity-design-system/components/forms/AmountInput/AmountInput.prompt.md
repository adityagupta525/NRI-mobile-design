AmountInput is the hero currency field for invest/SIP flows — auto-formats Indian digit grouping (₹1,23,456) as the user types, Tabular figures.

```jsx
<AmountInput label="Investment amount" value={amount} onChange={setAmount} helperText="Min. SIP ₹1,000" />
```
