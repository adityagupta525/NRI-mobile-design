SIPCalculatorSlider has amount + tenure sliders (copper track) and shows a projected value as an ESTIMATE only — tabular hero numeral with Indian grouping, asterisk gloss, and the market-risk disclaimer (Batch 2 `voice.disclaimer`). Never presents returns as guaranteed.

Motion: slider fill. Haptic: selection tick on step. Voice: `voice.disclaimer` + past-performance gloss.

```jsx
<SIPCalculatorSlider assumedReturn={12} />
```
