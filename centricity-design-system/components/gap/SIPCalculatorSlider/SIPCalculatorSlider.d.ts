export interface SIPCalculatorSliderProps {
  /** assumed annual return % used for the estimate (never presented as guaranteed) */
  assumedReturn?: number;
  disclaimer?: string;
}
export function SIPCalculatorSlider(props: SIPCalculatorSliderProps): JSX.Element;
