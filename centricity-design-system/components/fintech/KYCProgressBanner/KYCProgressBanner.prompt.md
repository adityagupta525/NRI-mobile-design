KYCProgressBanner is the dotted-texture grey banner pinned above the header while KYC is under review — title, "up to 48 hours" subtitle, and a NOTIFY ME toggle.

```jsx
<KYCProgressBanner notify onNotifyChange={setNotify} />
```
