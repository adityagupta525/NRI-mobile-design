BiometricPrompt is the Face ID / fingerprint unlock content, shown inside a BottomSheet to authorize orders/login. Carries the one copper rim-glow.

```jsx
<BottomSheet><BiometricPrompt type="faceid" /></BottomSheet>
```
