DocUploadCard is the KYC document slot (PAN card, Aadhaar, signature, selfie) — dashed border when empty, sage check when uploaded.

```jsx
<DocUploadCard label="PAN card" hint="Clear photo, all corners visible" />
<DocUploadCard label="Aadhaar card" uploaded />
```
