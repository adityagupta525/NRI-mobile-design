EmptyState fills a no-data view — empty portfolio, no transactions yet, no search results — with an icon, message, and optional CTA.

```jsx
<EmptyState title="No investments yet" description="Start your first SIP to build your India portfolio." action={<Button>Explore funds</Button>} />
```
