Tag is a label/filter token — fund categories, applied filters (dismissible). Copper-only: `neutral` (glass) or `copper` accent, never green/brick (those are reserved for gain/loss data). Distinct from StatusChip (compliance/order state).

```jsx
<Tag tone="copper">Flexi Cap</Tag>
<Tag onDismiss={() => remove()}>Equity</Tag>
```
