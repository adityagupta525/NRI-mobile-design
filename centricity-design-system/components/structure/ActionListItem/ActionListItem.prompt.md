ActionListItem is the base tappable row — settings screens, fund lists, notifications — with leading icon/avatar, title+subtitle, trailing chevron or custom control.

```jsx
<ActionListItem leading={<Icon />} title="Bank accounts" subtitle="2 linked" />
```
